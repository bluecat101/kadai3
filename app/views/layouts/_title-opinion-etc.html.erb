<td><%= link_to "#{book.title}" ,book_path(book.id) %></td>
<td><%= book.body%></td>
<td><%=render partial: "favorites/favorites",locals: {book: book}%></td>
<td><%=render partial: "book_comments/comment",locals: {book: book}%></td>